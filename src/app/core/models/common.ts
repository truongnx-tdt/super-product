export enum Common {
}

export enum RouteConstants {
    LOGIN = 'login',
    SIGNUP = 'signup',
    DASHBOARD = 'dashboard',
    SETTINGS = 'settings',
    PROFILE = 'profile',
    HOME = '',
    NOT_FOUND = 'not-found',
    FORBIDDEN = 'forbidden',
    ABOUT = 'about',
    EMPTY = ''
}

export enum StorageKey {
    AT = "accessToken",
    RT = "rfToken",
}